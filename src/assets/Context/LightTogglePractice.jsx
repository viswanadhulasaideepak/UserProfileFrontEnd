import React, { useState } from 'react'

export default function LightTogglePractice() {
    //const [light,setLight]=useState(true)
    const [data,setData]=useState(true)
  return (
    /*<div style={{backgroundColor:light ? "yellow":"orange",
        color: light? "black":"white",
        width: "300px",
        height: "300px",
        padding: "5px",
        textAlign:"center",
        borderRadius:"13px",
        boxShadow: "10px inset aqua"
    }}>
        <h1>{light ? "Light Mode": "Dark Mode"}</h1>
        <button onClick={()=>setLight(!light)}>Switch to {light? "dark mode": "light mode"}</button>
      
    </div>*/
    <div>
    </div>
  )
}
